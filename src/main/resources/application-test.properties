# ========================================
# TEST ENVIRONMENT CONFIGURATION
# ========================================
# This file is used when running tests or with: spring.profiles.active=test

# Application Name
spring.application.name=Blogging Platform - Test

# Server Configuration
server.port=8081

# Database Configuration - Test Database
# BEGINNER NOTE: Use a separate test database to avoid affecting your development data
spring.datasource.url=jdbc:postgresql://localhost:5432/Blogging_platform2_test
spring.datasource.username=postgres
spring.datasource.password=Annet@25437
spring.datasource.driver-class-name=org.postgresql.Driver

# Logging Configuration - Minimal for tests
logging.level.root=WARN
logging.level.com.example.Blogging_platform2=INFO

# OpenAPI/Swagger Configuration - Disabled in test
springdoc.swagger-ui.enabled=false

# GraphQL Configuration - Disabled in test
spring.graphql.graphiql.enabled=false
